{% extends 'backOffice/base.html.twig' %}
{% block title %}Newsletter{% endblock %}
{% block body %}

    {{ form_start(form) }}
    <fieldset class="uk-fieldset">

        <legend class="uk-legend">Ajouter un evenement</legend>
        <div class="uk-margin">
            {{ form_label(form.name, 'Sujet des émails') }}
            {{ form_widget(form.name) }}
            {{ form_errors(form.name) }}
        </div>
        <div class="uk-margin">
            {{ form_label(form.content, 'Contenu') }}
            {{ form_widget(form.content) }}
            {{ form_errors(form.content) }}
        </div>
        <button type="submit" class="uk-button uk-button-primary">Envoyer</button>
    </fieldset>
    {{ form_end(form) }}

{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('ckeditor/adapters/jquery.js') }}" type="text/javascript"></script>
    <script src="{{ asset('ckeditor/ckeditor.js') }}" type="text/javascript"></script>
    <script type="text/javascript">
        $(function() {
            $('textarea.ckeditor').ckeditor();
        });
    </script>
{% endblock %}